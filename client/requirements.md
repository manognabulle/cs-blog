## Packages
framer-motion | Smooth page transitions and component animations
lucide-react | Beautiful icons for the UI
react-hook-form | Form state management
zod | Schema validation
@hookform/resolvers | Zod resolver for react-hook-form

## Notes
Using Google Fonts: 'Outfit' for headings and 'Inter' for body text.
Theme toggle implemented using Tailwind's dark mode class strategy.
Images are sourced from Unsplash with descriptive alt text.
API endpoints: /api/posts, /api/posts/:slug, /api/messages
